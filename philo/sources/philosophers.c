#include "../includes/philosophers.h"

int main(int argc, char *argv[])
{
    int             status_code;
    t_philo_info    *info;


    exit_code = 0;
    try_parse_info(&info, &status_code);
    if (code == -1)
    {
        free_memory();
    }
    return (0);
}
